{
  "name": "photo-walkthrough-world",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "mint:sync": "node scripts/sync-mint-assets.mjs --project ."
  },
  "dependencies": {
    "@sparkjsdev/spark": "^2.1.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^5.9.3",
    "vite": "^6.4.3"
  }
}
